"use strict";

function _createForOfIteratorHelper(e, t) {
  var _n;

  if ("undefined" == typeof Symbol || null == e[Symbol.iterator]) {
    if (Array.isArray(e) || (_n = _unsupportedIterableToArray(e)) || t && e && "number" == typeof e.length) {
      _n && (e = _n);

      var r = 0,
          a = function a() {};

      return {
        s: a,
        n: function n() {
          return r >= e.length ? {
            done: !0
          } : {
            done: !1,
            value: e[r++]
          };
        },
        e: function e(_e) {
          throw _e;
        },
        f: a
      };
    }

    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }

  var o,
      i = !0,
      s = !1;
  return {
    s: function s() {
      _n = e[Symbol.iterator]();
    },
    n: function n() {
      var e = _n.next();

      return i = e.done, e;
    },
    e: function e(_e2) {
      s = !0, o = _e2;
    },
    f: function f() {
      try {
        i || null == _n["return"] || _n["return"]();
      } finally {
        if (s) throw o;
      }
    }
  };
}

function _unsupportedIterableToArray(e, t) {
  if (e) {
    if ("string" == typeof e) return _arrayLikeToArray(e, t);
    var n = Object.prototype.toString.call(e).slice(8, -1);
    return "Object" === n && e.constructor && (n = e.constructor.name), "Map" === n || "Set" === n ? Array.from(e) : "Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n) ? _arrayLikeToArray(e, t) : void 0;
  }
}

function _arrayLikeToArray(e, t) {
  (null == t || t > e.length) && (t = e.length);

  for (var n = 0, r = new Array(t); n < t; n++) {
    r[n] = e[n];
  }

  return r;
}

jQuery(document).ready(function (e) {
  e(".wuqi-dropdown-arrow").on("click", function (t) {
    t.preventDefault(), e(this).toggleClass("active"), e(this).next().slideToggle();
  }), function (t) {
    var n = e(t).find('a[href]:not([disabled]), button:not([disabled]), textarea:not([disabled]), input[type="text"]:not([disabled]), input[type="radio"]:not([disabled]), input[type="checkbox"]:not([disabled]), select:not([disabled])'),
        r = n[0],
        a = n[n.length - 1];
    e("#site-navigation").on("keydown", function (t) {
      e("body").hasClass("flyout-active") && ("Tab" === t.key || 9 === t.keyCode) && (t.shiftKey ? document.activeElement === r && (a.focus(), t.preventDefault()) : document.activeElement === a && (r.focus(), t.preventDefault()));
    });
  }("#site-navigation");
}), function () {
  function e() {
    if ("focus" === event.type || "blur" === event.type) for (var e = this; !e.classList.contains("nav-menu");) {
      "li" === e.tagName.toLowerCase() && e.classList.toggle("focus"), e = e.parentNode;
    }

    if ("touchstart" === event.type) {
      var t,
          n = this.parentNode,
          r = _createForOfIteratorHelper(n.parentNode.children);

      try {
        for (r.s(); !(t = r.n()).done;) {
          var a = t.value;
          n !== a && a.classList.remove("focus");
        }
      } catch (e) {
        r.e(e);
      } finally {
        r.f();
      }

      n.classList.toggle("focus");
    }
  }

  var t = document.getElementById("site-navigation");

  if (t) {
    var n = t.getElementsByTagName("button")[0];

    if (void 0 !== n) {
      var r = t.getElementsByTagName("ul")[0];
      if (void 0 === r) return void (n.style.display = "none");
      r.classList.contains("nav-menu") || r.classList.add("nav-menu"), n.addEventListener("click", function () {
        t.classList.toggle("toggled"), document.body.classList.toggle("flyout-active"), "true" === n.getAttribute("aria-expanded") ? n.setAttribute("aria-expanded", "false") : n.setAttribute("aria-expanded", "true");
      }), document.addEventListener("click", function (e) {
        t.contains(e.target) || (t.classList.remove("toggled"), n.setAttribute("aria-expanded", "false"));
      });

      var a,
          o = r.getElementsByTagName("a"),
          i = r.querySelectorAll(".menu-item-has-children > a, .page_item_has_children > a"),
          s = _createForOfIteratorHelper(o);

      try {
        for (s.s(); !(a = s.n()).done;) {
          var l = a.value;
          l.addEventListener("focus", e, !0), l.addEventListener("blur", e, !0);
        }
      } catch (e) {
        s.e(e);
      } finally {
        s.f();
      }

      var u,
          c = _createForOfIteratorHelper(i);

      try {
        for (c.s(); !(u = c.n()).done;) {
          u.value.addEventListener("touchstart", e, !1);
        }
      } catch (e) {
        c.e(e);
      } finally {
        c.f();
      }
    }
  }
}();